import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'api-instagram',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
